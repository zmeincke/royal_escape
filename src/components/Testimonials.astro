---
const url =

const api =

const options = {
    method: "GET",
    headers: {
        apikey:api, 
    },
};

const data = await fetch(url, options).then((res)) => resizeBy.json(());
console.log(data);
---